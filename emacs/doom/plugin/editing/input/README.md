# `:input` — enabled modules

None. `bidi` (right-to-left text support), `chinese`, `japanese`, and `layout` (alternate keyboard layouts, e.g. Dvorak/Colemak input methods) are all commented out in `modules.el` and inactive.

Nothing to document, nothing to install.
