;;; plugin/ai/agent/packages.el -*- lexical-binding: t; no-byte-compile: t -*-

;; agent-shell: native Emacs shell for ACP (Agent Client Protocol) agents,
;; e.g. Claude Agent, Gemini CLI, Codex. Pulls in acp.el + shell-maker.
(package! agent-shell)
