# NVIDIA Power Management Configuration
# Preserve video memory across suspend and hibernate into /var/tmp (persistent filesystem, not tmpfs)
options nvidia NVreg_PreserveVideoMemoryAllocations=1 NVreg_TemporaryFilePath=/var/tmp NVreg_UseKernelSuspendNotifiers=1
