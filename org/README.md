# org

Org-mode data (agenda files, notes, captures) - `org-directory` in
`emacs/doom/plugin/org/config.el` stays `~/org/`; `emacs/emacs-install.sh`
symlinks that to this directory so the files themselves are git-synced
along with the rest of the dotfiles.
